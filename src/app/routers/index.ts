import { Router } from "express";

const router = Router();

// const moduleRouters = [
// //   {
// //     path: "/admins",
// //     route: AdminRoutes,
// //   },
// ];

// moduleRouters.forEach((router) => router.use(router.push, router.route));

// export default router;
